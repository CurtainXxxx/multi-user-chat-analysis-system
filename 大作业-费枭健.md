# 《Python 语言程序设计》期末综合大作业说明书

**学号：** 202408784110 &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; **姓名：** 费枭健 &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; **班级：** 24软件创新一班

**作品评阅成绩表**

| 类别 | 技术综述 | 系统流程设计 | 系统详细设计与实现 | 功能拓展 | 总结与建议 | 排版 | PPT、视频、词云图 | 代码设计 | 总分 | 总评人 | 复查人 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 分值 | 5 | 5 | 25 | 10 | 5 | 10 | 15 | 25 | 100 | 彭增焰 | 吴东 |
| 得分 | | | | | | | | | **0** | | |

<br>

**作业题目：** 多人在线聊天与智能分析系统

---

## 1 技术综述

本学期《Python 语言程序设计》课程涵盖了从基础语法到高级应用的多个技术领域。相比于前半学期零散的代码练习，本次"多人在线聊天与智能分析系统"大作业让我真正体会到了如何将六项核心技术（Socket通信、多线程、网页爬虫、正则匹配、分词与词云、文件操作）拧成一股绳，构建一个完整的后端服务。

在这次系统的底层通信实现中，我重点使用了 Socket 和 threading 模块。起初在编写单线程的 Socket 测试时，我发现 accept() 和 recv() 的阻塞机制会导致服务端一次只能干一件事——要么死等新连接，要么死等新消息，这在多人聊天场景下根本行不通。引入多线程后，我让主线程专职负责"揽客"（监听连接），把耗时的消息收发逻辑全部甩给子线程处理，这才真正让系统"并发"跑了起来。在这个过程中，我也实际体会到了 Python 全局解释器锁（GIL）的特点，意识到对于网络 I/O 密集型任务，多线程确实是极佳的解决方案。

在数据获取与处理层面，我整合了 requests 爬虫、re 正则表达式以及 jieba 分词。实际敲代码时我深刻意识到：现实网络中的数据远比教科书上的示例要"脏"得多。直接用爬虫拿到的 HTML 源码如果直接丢给 jieba，分出来的全是无意义的标签碎片。为了拿到干净的文本，我不得不反复测试并重写正则表达式，最终摸索出了一套"先去脚本样式、再去标签实体"的多层过滤流水线。至于词云可视化，虽然 wordcloud 库调用简单，但在中文字体路径的配置和停用词过滤（比如剔除"的"、"了"、"是"）上，也着实费了一番功夫调试。总体而言，这六项技术在项目中并非孤立存在，而是形成了"通信-获取-清洗-分析-存储"的完整数据流闭环。

---

## 2 系统流程设计

### 2.1 系统整体架构

本系统提供两种运行模式：**终端版**（C/S 架构，TCP Socket + 多线程）和 **Web 版**（B/S 架构，FastAPI + WebSocket + 浏览器前端）。图1展示了Web版的系统整体架构。

```
                        ┌──────────────────────────────────┐
                        │         浏览器 (前端)              │
                        │  ┌────────────────────────────┐  │
                        │  │  index.html                │  │
                        │  │  ┌──────────┐  ┌────────┐  │  │
                        │  │  │ 消息区    │  │ 在线    │  │  │
                        │  │  │ (实时滚动)│  │ 用户列表│  │  │
                        │  │  ├──────────┤  │        │  │  │
                        │  │  │ 输入框    │  │ 暗色/  │  │  │
                        │  │  │ [发送按钮] │  │ 亮色主题│  │  │
                        │  │  └──────────┘  └────────┘  │  │
                        │  └────────────────────────────┘  │
                        └────────────┬─────────────────────┘
                                     │  WebSocket
                                     │  ws://localhost:8888/ws
                                     ▼
                        ┌──────────────────────────────────┐
                        │     FastAPI 服务端 (Python)        │
                        │  ┌────────────────────────────┐  │
                        │  │  WebSocket 连接管理          │  │
                        │  │  - 多用户并发 (asyncio)      │  │
                        │  │  - 消息广播/私聊(@)          │  │
                        │  │  - 正在输入提示推送          │  │
                        │  │  - 在线用户列表实时同步      │  │
                        │  ├────────────────────────────┤  │
                        │  │  指令处理器                  │  │
                        │  │  - !stat  → 词频+词云        │  │
                        │  │  - !crawl → 爬虫+分析        │  │
                        │  │  - !quit  → 退出             │  │
                        │  ├────────────────────────────┤  │
                        │  │  数据持久化                  │  │
                        │  │  - chat_log.json (聊天记录)  │  │
                        │  │  - 词云图-费枭健.png          │  │
                        │  └────────────────────────────┘  │
                        └──────────────────────────────────┘
```

[图1] 系统整体架构图

### 2.2 系统工作流程

系统的核心工作流程分为以下几个阶段：

**(1) 服务端启动阶段：** 启动 FastAPI + Uvicorn 服务器，监听 `0.0.0.0:8888`。初始化 `ConnectionManager`，准备接收 WebSocket 连接。

**(2) 用户登录阶段：** 用户通过浏览器访问 `http://localhost:8888`，服务端返回聊天页面。用户输入真实姓名后，前端通过 WebSocket 发送用户名到服务端。服务端核验用户名，检测重名冲突，通过后加入在线用户列表，向所有用户广播加入通知并推送最新在线列表。新用户会收到最近的聊天历史记录。

**(3) 消息通信阶段：** 用户输入消息并发送，服务端接收后添加时间戳，将消息写入 JSON 文件持久化，然后广播给其他在线用户，同时回显给发送者本人。消息类型包括：普通聊天（蓝色/白色气泡）、@私聊（紫色虚线气泡）、系统通知（黄色横幅）、分析结果。

**(4) 指令处理阶段：** 服务端通过正则表达式识别特殊指令。`!stat` 触发词频统计与词云生成；`!crawl <URL>` 触发网页爬取与内容分析；`!quit` 触发用户退出流程。

**(5) 用户退出阶段：** 用户发送 `!quit` 或直接关闭浏览器。服务端从连接字典中移除该用户，向所有在线用户广播离开通知，推送更新后的在线列表，正常关闭 WebSocket 连接。

系统总体工作流程如图2所示。

```
  服务端启动               用户登录                消息/指令循环
      │                      │                      │
      ▼                      ▼                      ▼
  ┌──────────┐         ┌──────────┐          ┌──────────┐
  │FastAPI   │         │输入姓名    │          │wait      │
  │Uvicorn   │         │WebSocket  │          │receive() │
  │监听8888  │         │发送用户名  │          │等待消息  │
  └────┬─────┘         └────┬─────┘          └────┬─────┘
       │                    │                     │
       ▼                    ▼                     ▼
  ┌──────────┐         ┌──────────┐         ┌──────────┐
  │等待连接  │         │重名检测   │         │正则匹配   │
  │WebSocket │         │发送历史   │         │指令?     │
  └────┬─────┘         │广播加入   │         └──┬───┬──┘
       │               └──────────┘            │   │
       │                                    是│ 否│
       ▼                                       │   │
  ┌──────────┐                                 │   ▼
  │为每连接  │                                 │ ┌──────────┐
  │创建协程  │                                 │ │保存到文件 │
  │加入字典  │                                 │ │广播给他人 │
  └──────────┘                                 │ │回显给自己 │
                                               │ └──────────┘
       ┌────────────────┬──────────────────────┘
       ▼                ▼
  ┌────────┐      ┌────────┐
  │!stat   │      │!crawl  │
  │词频统计│      │爬取网页│
  │生成词云│      │Top10词 │
  │Top5高频│      │发结果  │
  └────────┘      └────────┘
       │                │
       └────┬───────────┘
            ▼
       ┌────────┐
       │!quit   │
       │广播离开│
       │更新列表│
       │关闭连接│
       └────────┘
```

[图2] 系统工作流程图

---

## 3 系统详细设计与实现

### 3.1 服务端核心模块

Web 版服务端基于 FastAPI 框架构建，使用 Uvicorn 作为 ASGI 服务器。相比终端版的 TCP Socket 模型，Web 版的优势在于：天然支持浏览器客户端、HTTP 路由和 WebSocket 端点可共存、异步并发效率更高。

```python
from fastapi import FastAPI, WebSocket, WebSocketDisconnect
import uvicorn

app = FastAPI(title="多人在线聊天与智能分析系统")

@app.websocket("/ws")
async def websocket_endpoint(ws: WebSocket):
    await ws.accept()                    # 完成 WebSocket 握手
    username = await ws.receive_text()   # 接收用户名
    manager.connect(username, ws)        # 加入连接字典
    # ... 消息循环 ...
```

`ConnectionManager` 类使用字典 `{username: WebSocket}` 管理所有在线连接，提供 `send_to()`、`broadcast()`、`broadcast_users()` 等方法。相比终端版的 `clients` 列表 + `threading.Lock`，asyncio 的单线程协程模型天然避免了竞态条件，无需显式加锁。

### 3.2 用户登录与重名检测

用户输入的姓名通过 WebSocket 发送给服务端。服务端首先检查用户名是否已被占用，若重名则返回错误信息并关闭连接，原用户不受影响；若可用则加入连接字典并广播。

```python
if username in manager.connections:
    await ws.send_text(json.dumps({
        "type": "system", "content": f'❌ 用户名 "{username}" 已被占用'
    }))
    await ws.close()
    return
manager.connect(username, ws)
```

前端收到重名消息后，通过 alert 弹窗提示用户并退回登录界面。

### 3.3 历史记录同步

新用户进入聊天室时，服务端从 `chat_log.json` 加载最近 50 条历史消息，逐条以 `type: "chat"` 格式发送给新用户，通过 `self` 字段区分自己的消息和其他人的消息，使新加入的用户了解之前的讨论内容。

```python
history = load_chat_log()
if history:
    for record in history[-50:]:
        await ws.send_text(json.dumps({
            "type": "chat", "user": record["user"],
            "time": record["time"], "content": record["content"],
            "self": record["user"] == username
        }))
```

### 3.4 消息广播与@私聊

系统支持三种消息投递方式：`broadcast()` 广播给除发送者外的所有人（用于普通聊天），`broadcast_all()` 广播给所有人（用于系统通知），`send_to()` 发送给指定用户（用于@私聊）。

@私聊通过正则 `r'^@(\S+)\s+(.+)'` 解析，提取目标用户名和私聊内容。如果目标用户在线，消息仅发送给目标用户和发送者本人；如果目标不在线，给发送者返回错误提示。私聊消息使用 `type: "whisper"` 类型，前端以紫色虚线边框样式渲染。

```python
at_match = re.match(r'^@(\S+)\s+(.+)', raw_msg)
if at_match:
    target, private_msg = at_match.group(1), at_match.group(2)
    if target in manager.connections:
        # 仅发送给目标用户和发送者
        await manager.send_to(target, ...)
        await manager.send_to(username, ...)
```

### 3.5 在线用户列表实时推送

用户加入或退出时，服务端调用 `broadcast_users()` 向所有在线用户推送独立的 `userlist` 消息，前端收到后立即更新侧栏在线用户列表。相比将用户列表附加在系统消息中，独立推送避免了消息顺序问题导致列表更新遗漏。

```python
async def broadcast_users(self):
    users = self.get_users()
    data = json.dumps({"type": "userlist", "users": users})
    for name, ws in list(self.connections.items()):
        await ws.send_text(data)
```

前端点击在线用户的名称，输入框自动补全 `@用户名 ` 前缀，方便发起私聊。

### 3.6 网页爬虫与内容分析模块

`crawl_and_analyze()` 函数使用 `requests` 获取网页内容，`BeautifulSoup` 解析 HTML，多层正则清洗标签和实体，`jieba` 中文分词并统计 TOP10 高频词。

在健壮性方面进行了多项增强：使用 HTTPS 连接池和证书忽略以避免 SSL 错误；设置 15 秒超时并支持最多 2 次自动重试（每次切换不同的 User-Agent）；对 HTTP 状态码进行区分处理（403 拒绝访问、4xx/5xx 等）；捕获并反馈具体的错误原因（连接超时、SSL 错误、读取超时等）。

```python
for attempt in range(2):
    headers = {'User-Agent': ua_list[attempt % len(ua_list)]}
    try:
        resp = requests.get(url, headers=headers, timeout=15,
                          allow_redirects=True, verify=False)
        # 分词 + 词频统计
        words = jieba.lcut(text)
        top10 = sorted(freq.items(), ...)[:10]
        return text[:2000], top10, len(words)
    except requests.exceptions.ConnectTimeout:
        last_error = "连接超时，网站可能无法访问"
    except requests.exceptions.SSLError:
        url = url.replace('https://', 'http://', 1)  # 降级重试
```

### 3.7 词频统计与词云生成模块

`generate_wordcloud()` 函数加载 `chat_log.json` 中的所有聊天内容，使用 `jieba` 分词，过滤停用词（含常用中文停用词及指令关键词），统计词频 TOP5，使用 `wordcloud` 库生成词云图保存为 PNG 文件。前端通过 `/wordcloud` 端点获取图片并展示在消息区。

```python
wc = wordcloud.WordCloud(
    font_path='C:/Windows/Fonts/msyh.ttc',
    width=800, height=600, background_color='white'
)
wc.generate_from_frequencies(freq)
wc.to_file(WORDCLOUD_FILE)
```

### 3.8 文件存储模块

聊天记录以 JSON 格式持久化。`save_chat_log()` 采用"加载→追加→全量写入"模式，`load_chat_log()` 在启动和 !stat 时调用。选择 JSON 的原因是与 Python 字典天然对应、结构清晰、支持中文（`ensure_ascii=False`）。

### 3.9 正则表达式应用

本系统在两个关键环节使用了正则表达式：指令解析和 HTML 文本清洗。

指令解析方面，主要依靠正则来捕获用户的输入意图。例如，使用 `re.match(r'^!crawl\s+', raw_msg, re.IGNORECASE)` 来精准拦截爬虫指令，并利用 `re.sub` 动态提取出后面的 URL 参数。

HTML 清洗流水线方面，这是实现网页爬虫模块时耗时最长的一个环节。起初，我以为用 requests 拿到 HTML 源码后，随便写个 `r'<[^>]+>'` 把尖括号里的标签删掉就可以直接进行词频统计了。但实际运行后发现，提取出来的文本里混杂了大量 JavaScript 代码和 CSS 样式，直接把 jieba 分词的统计结果完全污染了。

经过反复 Debug 和查阅文档，我意识到必须重构清洗逻辑。最终我设计了一套"逐级剥离"的多层正则流水线：

```python
def clean_html(html):
    # 第1层：必须带上 re.DOTALL，否则无法匹配多行的 script 和 style 代码块内部内容
    clean = re.sub(r'<script[^>]*>.*?</script>', '', html, flags=re.DOTALL)
    clean = re.sub(r'<style[^>]*>.*?</style>', '', clean, flags=re.DOTALL)
    # 第2层：去除常规 HTML 标签
    clean = re.sub(r'<[^>]+>', '', clean)
    # 第3层：去除 HTML 转义实体（如 &nbsp; &amp;）
    clean = re.sub(r'&[a-zA-Z]+;', ' ', clean)
    clean = re.sub(r'&#?\w+;', ' ', clean)
    # 第4层：将多余的换行和空白字符合并为单空格
    clean = re.sub(r'\s+', ' ', clean).strip()
    return clean
```

这段踩坑经历让我深刻认识到：现实业务中的数据清洗极其繁杂。正则表达式虽然语法晦涩、写起来费劲，但在这种急需精细化文本过滤的关头，确实是无可替代的利器。

---

## 4 功能拓展

### 4.1 多人在线并发通信

在完成基础的客户端-服务端单线通信后，我进一步将系统拓展为支持任意人数并发的多人聊天室。这不仅要求服务端具备线程池管理能力，更需要严谨地处理"线程安全"问题。

在编码初期，我使用了一个全局的 clients 列表来保存所有在线用户的 Socket 对象。但在构思极端测试场景时我意识到一个隐患：如果客户端 A 和客户端 B 在完全相同的毫秒级时间点接入服务器或断开连接，两个子线程会同时对 clients 列表执行 append 或 remove 操作。在并发环境下，这极大概率会引发竞态条件（Race Condition），导致列表数据结构损坏或消息发错人。

为了防患于未然，我显式地引入了线程锁 threading.Lock()。在所有的广播函数（broadcast）和列表增删逻辑外围，严格加上了 `with clients_lock:` 上下文管理器。虽然加锁在微观上会略微增加一点排队开销，但这彻底杜绝了多线程并发修改导致的数据不一致问题，保证了服务端在高频聊天场景下的绝对稳定。

```python
clients = []
clients_lock = threading.Lock()

# 广播给除发送者外的所有人
def broadcast(message, sender_conn=None):
    with clients_lock:
        for name, conn in clients:
            if conn != sender_conn:
                conn.send(message.encode('utf-8'))
```

### 4.2 Web 前端界面

在原终端版基础上，使用 HTML + CSS + JavaScript 构建了完整的 Web 聊天界面，包含：左侧在线用户侧栏、中间消息滚动区域、底部输入区和指令按钮。前端通过 WebSocket 与服务端进行实时双向通信。图3展示了聊天界面的布局。

```
┌──────────┬──────────────────────────────────┐
│ 在线用户  │   💬 多人在线聊天与智能分析系统     │
│          ├──────────────────────────────────┤
│ 🟢 费枭健 │  🟢 费枭健 加入了聊天室            │
│ 🟢 张三   │  [2026-06-24 15:30] 费枭健: 你好  │
│ 🟢 李四   │  [2026-06-24 15:31] 张三: 大家好  │
│          │  📈 TOP5高频词: Python(5次)...    │
│          ├──────────────────────────────────┤
│ 共3人在线 │  [输入消息...       ] [发送] [词频]│
└──────────┴──────────────────────────────────┘
```

[图3] 聊天界面布局示意

### 4.3 暗色/亮色主题切换

使用 CSS 变量（`:root` 和 `.dark`）定义两套配色方案，通过 JavaScript 一键切换 `<html>` 的 class。选择保存在 `localStorage` 中，刷新页面后保持。暗色模式下所有 UI 元素自动适配，包括消息气泡、输入框、侧栏、系统通知等。

```css
:root { --bg:#e8eef2; --msg-bg:#fff; ... }
.dark { --bg:#1a1a2e; --msg-bg:#1e3a5f; ... }
```

### 4.4 @私聊功能

用户可通过 `@用户名 消息内容` 格式发送私聊消息。服务端解析目标用户后，消息仅投递给目标用户和发送者本人，其他用户不可见。私聊消息以紫色虚线气泡展示，并有独特的提示音。点击在线用户列表中的名称，输入框自动补全 `@用户名 ` 前缀。

### 4.5 正在输入提示

用户在前端输入框中打字时，通过 `input` 事件触发 `!typing` 指令发送给服务端，服务端广播 `type: "typing"` 消息给其他用户。前端在消息区底部显示"XXX 正在输入..."的文字提示，2 秒无新打字则自动消失。此功能增强了聊天的实时临场感。

### 4.6 消息提示音

使用 Web Audio API 在浏览器端生成提示音，无需任何音频文件。收到普通消息时播放 800Hz 短音（叮），收到私聊时播放 1200Hz+1000Hz 双音（叮咚）。首次播放时自动激活 AudioContext（浏览器要求用户交互后才能播放音频）。

### 4.7 爬虫健壮性增强

对 `crawl_and_analyze()` 函数进行了多项健壮性增强：多 User-Agent 轮换、2 次自动重试、SSL 证书忽略（HTTPS 失败后降级 HTTP）、15 秒超时、区分 403/4xx/5xx 等状态码、捕获具体异常（连接超时/SSL 错误/读取超时/重定向过多）并返回明确的中文错误提示。

### 4.8 在线用户列表实时推送

将用户列表从系统消息中分离为独立的 `userlist` 消息类型，在用户加入、退出时主动推送，前端收到后立即更新侧栏列表。避免了消息顺序问题导致的列表显示延迟。

### 4.9 重名保护机制

服务端在用户连接时检查用户名是否已被占用，若重名则返回错误信息并关闭该连接，不影响已在线用户。前端弹窗提示用户更换名字重新登录。

**Debug 记录：**

测试时发现一个隐蔽的 bug——第一个窗口用"张三"登录正常，第二个窗口尝试用"张三"登录时被拒绝（符合预期），但之后第三个窗口用"李四"登录时，在线列表里看不到张三。

排查后发现根因在 `finally` 清理逻辑：

```python
# Bug 版本
finally:
    if username:                           # username 已被赋值为 "张三"
        manager.disconnect(username)       # 把真正的张三踢掉了！
        await manager.broadcast_users()
```

重名检测时，`username` 变量已被设为 `"张三"`，虽然 `manager.connect()` 没有执行，但 `finally` 块的 `manager.disconnect(username)` 仍然触发了，导致已在线用户张三的连接被误删。

**修复方案**：引入 `connected` 布尔标志位，只有 `manager.connect()` 成功执行后才设为 `True`，`finally` 块仅在 `connected=True` 时执行清理：

```python
# 修复版本
username = None
connected = False                         # 新增标志位
try:
    ...
    if username in manager.connections:
        ...
        return                            # connected 仍为 False
    manager.connect(username, ws)
    connected = True                      # 连接成功才标记
    ...
finally:
    if connected:                         # 只有成功连接的才清理
        manager.disconnect(username)
        await manager.broadcast_users()
```

这个 bug 反映出 `try/finally` 中条件判断粒度的重要性——不能仅凭变量是否赋值来决定清理逻辑，必须以实际操作是否成功为准。

---

## 5 总结与建议

### 5.1 技术总结

通过纯手工从零搭建这个包含通信、网络IO和NLP预处理的并发聊天室，我把本学期零散的语法点做了一次极好的"工程化"串联。

网络与并发的敬畏感：手写 Socket 让我直面了底层数据流的"粘包"问题（最终通过约定换行符 \n 解决），而多线程的引入让我养成了"凡遇全局变量必考虑加锁"的防御性编程思维。

文本处理的实操感：requests + BeautifulSoup 加上正则清洗的组合拳，让我明白了获取数据只是第一步，"洗数据"才是脏活累活。而结巴分词与 WordCloud 的结合，让我直观感受到了数据可视化的魅力。

架构拓展的思考：现在的技术圈正处于大模型（LLM）和智能体（Agent）爆发的阶段。这个聊天室其实已经具备了一个智能助手的雏形外壳。后续如果我把单纯的 !stat 词频统计升级，去对接一些外部大模型的 API，就能很方便地将其魔改为一个支持多用户的智能后端系统。

### 5.2 课程教学建议

经过一个学期的学习，我对课程提出以下建议：

增加防御性编程和底层机制的探讨：目前课程对基础语法的覆盖已经很棒。考虑到我们专业未来的工程需求，建议在多线程这部分，可以稍微深入探讨一下 Python 底层 GIL 锁的局限性，或者引入一点 asyncio 异步 I/O 的概念，这会极大满足同学们对底层运行机制的好奇心。

在作业中模拟真实"脏数据"场景：建议平时的实训作业可以故意给一些格式混乱的原始数据（比如错乱的日志文件、带有乱码的网页），让大家早点体会到正则清洗和异常捕获在真实项目中的重要性。

项目驱动前置：建议在课程中期就可以抛出类似本次大作业的简化版框架，让大家带着构建系统的目标去学习后续的文件操作和网络编程，这样成就感会更强。

---

**参考文献**

[1] Python Software Foundation. Python 3.10 Documentation[EB/OL]. https://docs.python.org/3.10/, 2024.

[2] 嵩天, 黄天羽, 礼欣. Python 语言程序设计基础(第2版)[M]. 北京: 高等教育出版社, 2017.

[3] Kenneth Reitz. Requests: HTTP for Humans[EB/OL]. https://docs.python-requests.org/, 2024.

[4] FastAPI. FastAPI Documentation[EB/OL]. https://fastapi.tiangolo.com/, 2024.
